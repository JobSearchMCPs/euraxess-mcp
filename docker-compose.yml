version: '3.8'
services:
  euraxess:
    build: ./euraxess
    image: yourrepo/euraxess-mcp:latest
    container_name: euraxess_mcp
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DEFAULT_LIMIT=100
